# importamos TensorFlow
import tensorflow as tf

# Imprimimos la version de TensorFlow 
print(tf.__version__)
 
nota_importante = """Nota: Si ven los requerimientos podran ver que la version de tenemos de TensorFlow
   es la pip install tensorflow-cpu que solo usa el procesar y la GPU y si tienes una GPU
   y los drivers necesarios, puedes usar la verion normal que se instala pip install tensorflow"""